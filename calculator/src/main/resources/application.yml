spring:
  application:
    name: Calculator
  jackson:
    serialization:
      fail-on-empty-beans: false
    default-property-inclusion: non_null
server:
  port: 8088
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
